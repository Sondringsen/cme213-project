\documentclass[11pt,letterpaper]{article}

\usepackage[margin=1in]{geometry}
\usepackage{amsmath, amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage{caption}
\usepackage{microtype}
\usepackage{parskip}
\usepackage{enumitem}

\setlist{itemsep=2pt, topsep=2pt, parsep=0pt}
\setlength{\parskip}{3pt plus 1pt minus 1pt}
\captionsetup{font=small, labelfont=bf, skip=3pt}

\graphicspath{{../plots/}{plots/}}
\hypersetup{colorlinks=true, linkcolor=blue, urlcolor=blue, citecolor=blue}

\title{\vspace{-2em}CME 213 Final Project\\
\large Building and Profiling a Small GPT-2 Training Pipeline in CUDA and MPI}
\author{Nils Astrup Toft \and Sondre Rogde}
\date{June 8, 2026}

\begin{document}
\maketitle\vspace{-1.5em}

\begin{abstract}
We build a GPT-2-style transformer training pipeline from scratch in C++/CUDA
with MPI data parallelism on 4$\times$Quadro RTX\,6000 (Turing, sm\_75). Profiling against the hardware roofline
reveals four training bottlenecks and a fifth inference regime. Register
tiling lifts GEMM from 13.5\% to 58.6\% of FP32 peak. \texttt{float4}
vectorization brings memory-bound kernels to 64--70\% of HBM peak. These two plus fusing
50 per-tensor \texttt{MPI\_Allreduce} calls into one pinned-buffer call
and pre-allocating backward scratch cut
4-GPU step time from 74.5\,ms to 59.8\,ms, raising strong-scaling
efficiency from 55\% to 62\% at $P{=}4$. A
PagedAttention decode kernel raises KV-cache utilization from 39\% to 88\%
at $B{=}64$ with $\leq$1.6\% indirection overhead and reaches 65\% of HBM
peak ($\approx$435\,GB/s), with outputs bit-identical to a contiguous
reference.
\end{abstract}

\section{Project Description}

We train a decoder-only GPT-2-style transformer~\cite{radford2019} on
sequences of token IDs. Inputs: a batch of $B$ integer sequences of length
$S$. Outputs: per-position vocabulary logits, reduced to a scalar
cross-entropy loss and backpropagated through every parameter with
Adam~\cite{kingma2015}. Each Pre-LayerNorm block applies multi-head causal
self-attention then a two-layer GELU FFN, both with residuals. Notation:
$B$ batch, $S$ sequence, $C$ model dim, $H$ heads, $D{=}C/H$, $V$ vocab.
All tensors FP32 row-major.

\paragraph{Setup.}
Default config: $n_\text{layers}{=}6$, $C{=}256$, $H{=}8$ ($D{=}32$),
$S{=}128$, $V{=}512$, which gives about 5\,M parameters, 20\,MB of gradient. We use
synthetic random tokens for scaling and profiling experiments and Wikitext-2 for realistic training dynamics. However, the main focus for the project is profiling and optimization analysis, not a competitive LLM. We use the class compute cluster which has peak FP32 16.31\,TFLOPS; peak HBM 672\,GB/s; ridge
$\approx$24\,FLOP/byte. A single forward
step has 36+ GEMMs and $O(S^2)$ attention in naive form. Using realistic batch
sizes easily outgrow single-GPU memory and per-step latency multiplies into hours
at full scale. This motivates both multi-GPU training and KV-cache
management for inference.

\section{Algorithms and State of the Art}
The below paragraphs discusses some of the algorithms and kernels we implemented. An overview of all implemented kernels can be found in the Appendix in Table \ref{tab:all_kernels}.

\paragraph{Dense GEMM.}
cuBLAS~\cite{cublas} reaches 70--80\% of FP32 peak via shared-memory
tiling, register-file accumulation, and (Ampere+) tensor cores. We
implement the shared-memory tiled variant from Boehm~\cite{boehm2022} with
$16{\times}16$ thread-block register tiling. This is a slightly more optimized version compared to the one we implemented in class as it uses a more aggressive register tiling. Turing tensor cores only support
FP16/INT8~\cite{micikevicius2018}, so we keep everything as FP32.

\paragraph{Attention.}
Flash Attention~\cite{dao2022} tiles $Q, K, V$ in SRAM with an online
softmax, reducing memory from $O(S^2)$ to $O(S{\cdot}D)$ at the same output.
We implement FA1 forward; the backward uses the naive $O(S^2)$
materialization. FA2's recomputation backward~\cite{dao2023} would be
needed for longer sequences.

\paragraph{The full transformer.}
We implement Pre-LayerNorm transformer blocks as in GPT-2~\cite{radford2019},
with Adam~\cite{kingma2015} as the optimizer.

\paragraph{Distributed training and inference.}
The standard approach is to replicate the model on each rank, have each rank
compute gradients on a local mini-batch, and synchronise via \texttt{MPI\_Allreduce}
before the optimizer step. The naive implementation issues one \texttt{MPI\_Allreduce}
per parameter tensor ($\approx$50 calls in our model) each carrying its own
MPI startup latency and a PCIe device-to-host round trip. Horovod~\cite{sergeev2018}
and Karpathy's \texttt{llm.c}~\cite{karpathy2024} instead pack all gradients into
a single contiguous buffer and issue one call. We implement the same strategy. More sophisticated methods with tensor parallelism (Megatron-LM~\cite{shoeybi2019}) can be used when models exceed single-GPU memory. We do not employ this as our 5\,M-parameter model fits trivially on 24\,GB. For inference, vLLM's
PagedAttention~\cite{kwon2023} manages variable-length KV caches via
virtual-memory-style block tables. We implement this for the decode step as discussed in the last lecture.

\section{Parallelization Strategy}

\paragraph{System overview.} We organize our system in five layers. CUDA kernels, stateful modules (Linear, LayerNormLayer,
EmbeddingLayer, MultiHeadAttention, TransformerBlock), \texttt{GPT2} model,
\texttt{Trainer} (forward $\to$ CE loss $\to$ backward $\to$ Adam), and MPI
wrapper.

\paragraph{GEMM (compute-bound).}
We implement three GEMM-variants ($AB$, $AB^T$, $A^TB$). Each uses a $128{\times}128$
output tile in shared memory with a $BK{=}8$ strip across $K$. The thread
block is $16{\times}16{=}256$ threads, each accumulating an $8{\times}8$
register sub-tile (64 FP32 accumulators per thread). The inner $k$-loop
is \texttt{\#pragma unroll}, exposing 64 independent FMAs per smem load, raising both arithmetic intensity and occupancy.
Global loads are coalesced. Shared-memory tiles are declared
\texttt{[TILE][TILE+1]}. The $+1$ padding breaks the stride that would
serialize warp writes onto a single shared-memory bank, eliminating
bank conflicts.

\paragraph{Flash Attention and bandwidth-bound kernels.}
Flash Attention tiles $Q, K, V$ into SRAM, maintaining a running max
$m^{(j)}$ and normalizer $\ell^{(j)}$ with online correction
$e^{m^{(j-1)}-m^{(j)}}$ (similar to what we did in the homework). Causal masking is per-tile; one block per
(batch, head, query-tile). LayerNorm uses a two-pass block reduction. The two passes were necessary to avoid catastrophic cancellation when computing the variance. GELU, Softmax, Cross-Entropy, the LayerNorm output
pass, and the residual \texttt{add\_inplace} use \texttt{float4} 128-bit
loads, which gives 4 floats per memory instruction, using the full 128-bit memory
bus. Adam is a pure read-modify-write of $\approx$20 bytes per parameter
and is bandwidth-dominated by construction.

\paragraph{PagedAttention decode kernel.}
Each block handles one (request, head) pair. The kernel reads $K, V$ via a per-request block table and runs the same Flash-Attention online-softmax recurrence. The indirection adds one integer lookup per
16 tokens, which is negligible against the $16{\cdot}D$-element $K/V$ tile that follows.

\paragraph{MPI data parallelism.}
Each rank owns a full model copy on one GPU. We use two collectives: \texttt{MPI\_Bcast} once at startup to synchronize initial weights, and \texttt{MPI\_Allreduce(MPI\_SUM)} after each backward pass to
average gradients across ranks. Batch scattering requires no MPI call. All ranks generate the same random shuffle from a shared step seed and each independently selects its own slice, so no data moves over
the network. Because all ranks then run an identical Adam update on the same averaged gradient, weights stay synchronized without a further broadcast.

\section{Performance Model}

Three complementary models, one per regime.

\paragraph{Roofline~\cite{williams2009}.}
Achievable performance is $\min(\text{peak FLOPS},\,B\cdot\text{intensity})$.
Basic tiled GEMM (one output per thread, $T{=}32$) has intensity $T/4{=}8$\,FLOP/byte,
sitting below the ridge and shared-memory-bandwidth-bound.
With $8{\times}8$ register tiling, intensity rises to 64\,FLOP/byte, above the ridge.
\emph{Prediction:} register tiling moves GEMM from memory- to compute-bound.
LayerNorm, GELU, Softmax, and Cross-Entropy have $O(1)$ intensity and stay
memory-bound; the lever is therefore vectorization.
The decode kernel sits at $\approx 0.5$\,FLOP/byte, far below the ridge.
With one block per (request, head), the grid has $H{\cdot}B$ blocks; for $H{=}8$
the grid fills 72 SMs only at $B \geq 9$, so small batches are SM-count-limited
before bandwidth even becomes relevant.

\paragraph{Communication and scaling models.}
We model allreduce latency as $T(n) = \alpha + \beta n$, where $\alpha$ captures
per-call startup and $\beta = 1/b$ is inverse bandwidth. $\alpha$ and $\beta$ are
fitted from the \texttt{allreduce\_bench} we made (see Section \ref{sec:bench}).
For our 20\,MB gradient, the bandwidth term predicts $\approx 7$\,ms per PCIe
transfer direction. Because our implementation is not CUDA-aware, each call
incurs a D$\to$H copy and an H$\to$D copy, adding $\approx 2n/b_\text{PCIe}
\approx 13$\,ms on top of the network cost.
The gap between the fitted curve and the ideal network bandwidth is therefore
this D$\to$H$\to$D round-trip cost, recoverable only with CUDA-aware MPI.


\paragraph{Amdahl and isoefficiency.}
For strong scaling with $T_P = T_\text{compute}/P + T_\text{comm}$, parallel
efficiency is $E(P) = T_1/(P\,T_P)$. Because $T_\text{comm}$ is fixed by
gradient size while $T_\text{compute}$ shrinks with $P$, efficiency degrades as
more GPUs are added. For isoefficiency, maintaining a fixed efficiency as $P$
grows, the total batch must scale proportionally with $P$.

\section{Benchmarking and Instrumentation}\label{sec:bench}

\paragraph{Tools.}
We use Nsight Compute (\texttt{ncu --set full}) to capture per-kernel
TFLOPS, GB/s, occupancy, and roofline coordinates, and Nsight Systems
to profile the full training timeline. Both runs capture all kernels
over 20 training steps.

\paragraph{Standalone benchmarks.}
\texttt{allreduce\_bench} fits $\alpha{+}\beta n$ for $P\in\{2,4\}$ across
256\,B--64\,MB message sizes (100-trial averages).
\texttt{paged\_attention\_bench} sweeps $B\in\{1,4,16,64,256\}$ on a
truncated-exponential length distribution (mean 64, capped at 128),
recording memory utilization analytically and decode latency via
\texttt{cudaEvent} (200-iteration averages).

We defer the results and comparison to the performance model to Sections \ref{sec:bottleneck}–\ref{sec:scalability}.

\section{Bottleneck Analysis}\label{sec:bottleneck}

Nsight Compute and Nsight Systems identify
four training bottlenecks and a fifth inference regime.

\textbf{(1) GEMM below the roofline ridge.} Without register tiling,
Nsight Compute places GEMM at 2.20\,TFLOPS = 13.5\% of FP32 peak and
8\,FLOP/byte, left of the $\approx$24\,FLOP/byte ridge
(Fig.~\ref{fig:roofline}). The kernel is memory-bound in shared memory, not global memory.
Each thread reads shared-memory tiles without accumulating enough FMAs per read to saturate the FP32 units.

\textbf{(2) Scalar loads on memory-bound kernels.} GELU, Softmax,
Cross-Entropy, and LayerNorm have $O(1)$ arithmetic intensity, so their
ceiling is HBM bandwidth. Nsight Compute reports GELU at
$\approx$114\,GB/s = 17\% of HBM peak before optimization
(Table~\ref{tab:kernels}).

\textbf{(3) Per-tensor allreduce.} 50 separate \texttt{MPI\_Allreduce}
calls per step each trigger a D$\to$H cudaMemcpy, a collective, and an
H$\to$D cudaMemcpy. Fig.~\ref{fig:comm} shows the measured latency vs.\
message size: latency is flat at small sizes (startup-dominated, $\alpha\approx51\,\mu$s per call for $P{=}2$) then grows with slope 1 on the log--log plot at large sizes ($\beta n$-dominated), implying an effective bandwidth of $\approx$3\,GB/s, constrained by the PCIe D$\to$H$\to$D round trip rather than the network.
The 50 calls cumulate to $\approx$18.6\,ms/step.

\textbf{(4) Per-step \texttt{cudaMalloc} in backward.} The Nsight
Systems timeline (Fig.~\ref{fig:cudamalloc}) shows $\approx$30\,ms of idle GPU
time per step as \texttt{cudaMalloc} barriers drain the kernel queue. The multi head attention and transformer block allocated 18 temporary buffers in each
call, including two $B{\cdot}H{\cdot}S^2$ attention-score matrices
($\approx$4\,MB each at $B{=}16$).

\textbf{(5) Decode has two regimes.} At $B{=}1$ the kernel is
parallelism-limited: 8 blocks (because of the 8 heads) for 72 SMs, giving 5.4\% HBM and 1.6\%
FP32 utilization. At $B{=}64$ it is bandwidth-bound at 65\% of HBM
peak (Table~\ref{tab:kernels}), confirming the roofline prediction that
a kernel at 0.5\,FLOP/byte is bandwidth-limited for large batches.
\section{Algorithmic Variants}

\paragraph{Variant 1: Register tiling.}
Extending one-output-per-thread to an $8{\times}8$ register sub-tile
(64 FP32 accumulators, $BM{=}BN{=}128$, $BK{=}8$, $16{\times}16$ thread
block) raises arithmetic intensity from 8 to 64\,FLOP/byte, which crosses the
hardware's ridge point of $\approx$24\,FLOP/byte and shifts the bottleneck from
shared-memory bandwidth to compute. Nsight Compute confirms that throughput rises from 2.20 to
9.56\,TFLOPS (13.5\%$\to$58.6\% of FP32 peak, $4.3\times$). cuBLAS
reaches 70--80\% on Turing without tensor cores; we attain $\sim$75\%
of that (Fig.~\ref{fig:roofline}, Table~\ref{tab:kernels}).

\paragraph{Variant 2: \texttt{float4} vectorized loads.}
Scalar 32-bit loads in GELU, Softmax, Cross-Entropy, LayerNorm, and
\texttt{add\_inplace} are replaced by \texttt{float4}, issuing one
128-bit transaction per four floats. Nsight Compute confirms the gain and
bandwidth rises to 430--471\,GB/s (64--70\% of HBM peak) across all five
kernels, versus scalar baselines of $\approx$16\% (LayerNorm, $\approx$110\,GB/s)
and $\approx$17\% (GELU, $\approx$114\,GB/s). This is a $\approx4\times$
improvement (Table~\ref{tab:kernels}) as expected.

\paragraph{Variant 3: Fused gradient allreduce.}
50 individual \texttt{MPI\_Allreduce} calls are replaced by one call on
a contiguous 20\,MB pinned buffer to eliminate redundant startup overhead.
Fitting $\alpha{+}\beta n$ to the \texttt{allreduce\_bench} data gives
$\alpha{=}51.2\,\mu$s for $P{=}2$, so eliminating 49 extra call startups
saves $49{\times}51.2\,\mu\text{s}\approx2.5\,\text{ms}$, consistent
with the measured $\approx$3\,ms gain. The saving is smaller than
anticipated because fusion does not reduce the total bytes crossing PCIe.
The full 20\,MB gradient still traverses D$\to$H$\to$D in both cases.
At $\approx$3\,GB/s effective PCIe staging bandwidth (Fig.~\ref{fig:comm}) that round trip costs $\approx$13\,ms regardless of call
count and cannot be recovered without CUDA-aware MPI to route gradients directly
over NVLink.

\paragraph{Variant 4: Pre-allocated backward scratch.}
Eighteen per-step buffers are moved from backward call sites to the
constructor, eliminating the $\approx$30\,ms of per-step cudaMalloc barriers
identified in Section~\ref{sec:bottleneck}. Figs.~\ref{fig:cudamalloc}
and~\ref{fig:cudamalloc_after} show the Nsight Systems timeline before and after.
The allocations and the resulting idle GPU time are absent in the optimized
profile.

\paragraph{Variant 5: PagedAttention for inference.}
Block-table indirection raises KV-cache utilization from 39.4\% to 88.0\%
at $B{=}64$ ($2.2\times$) with only 1.3\% latency overhead, allowing the
same memory budget to serve $2.2\times$ more concurrent requests
(Table~\ref{tab:paged}).

\section{Scalability Analysis}

Table~\ref{tab:scaling} reports strong and weak scaling before and after
applying all four variants. At $P{=}1$ there is no allreduce, so Variant~3
contributes zero; the 14\,ms reduction from 162.7 to 148.8\,ms reflects
Variants 1, 2, and 4 combined (no intermediate measurements were taken to
isolate Variant~4). At $P{=}4$, all four variants together drop step time
from 74.5 to 59.8\,ms: communication falls 18.6$\to$15.6\,ms from fusing
(V3) and compute falls 55.9$\to$44.2\,ms from kernel and allocation
optimizations (V1+V2+V4), raising strong-scaling efficiency from 55\% to
62\%.

\begin{table}[h]
  \centering\small\setlength{\tabcolsep}{5pt}
  \begin{tabular}{@{}llrrrrrr@{}}
    \toprule
    \textbf{Config} & \textbf{Sweep} & $P$ &
    \textbf{step (ms)} & \textbf{comm (ms)} & \textbf{comm\%} &
    \textbf{speedup} & \textbf{eff.} \\
    \midrule
    Before opt. & strong & 1 & 162.7 & 0.0  & ---   & $1.00\times$ & 100\% \\
    Before opt. & strong & 2 & 105.1 & 17.7 & 16.8  & $1.55\times$ & 77\%  \\
    Before opt. & strong & 4 &  74.5 & 18.6 & 25.0  & $2.19\times$ & 55\%  \\
    \midrule
    After opt.  & strong & 1 & 148.8 & 0.0  & ---   & $1.00\times$ & 100\% \\
    After opt.  & strong & 2 &  92.5 & 14.4 & 15.6  & $1.61\times$ & 80\%  \\
    After opt.  & strong & 4 &  59.8 & 15.6 & 26.1  & $2.49\times$ & 62\%  \\
    \midrule
    After opt.  & weak   & 1 &  43.6 & 0.0  & ---   & ---          & 100\% \\
    After opt.  & weak   & 2 &  57.9 & 14.3 & 24.6  & ---          & 75\%  \\
    After opt.  & weak   & 4 &  59.8 & 15.6 & 26.1  & ---          & 73\%  \\
    \bottomrule
  \end{tabular}
  \caption{Scaling on one 4-GPU node (RTX\,6000, sm\_75). Strong: total
    $B{=}16$. Weak: local $B{=}4$ per rank. $n_\text{layers}{=}6$,
    $C{=}256$, $S{=}128$, 20-step averages.}
  \label{tab:scaling}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{strong_scaling}
  \caption{Strong scaling: step-time breakdown into compute
    (forward + backward + Adam) and communication
    (\texttt{allreduce\_gradients}). Total $B{=}16$, 20-step averages.}
  \label{fig:scaling}
\end{figure}

\paragraph{Amdahl and isoefficiency.}
At $P{=}4$ after optimization, 44.2\,ms is compute and 15.6\,ms is allreduce
(26\% of step time), giving $E_4 = T_1/(4T_4) = 148.8/(4\times59.8) \approx 62\%$.
The compute/comm ratio is $T_\text{compute}/T_\text{comm} = 44/15.6 \approx 2.8$.
We can increase this ratio in two ways. First, we can increase the local batch (more compute per
allreduce call), or second, we can adopt CUDA-aware MPI to cut comm to $\sim$7\,ms
(removing PCIe staging), giving ratio $44/7 \approx 6.3$.

\paragraph{Weak scaling.}
Efficiency at fixed $B_\text{local}{=}4$ is 75\% ($P{=}2$) and 73\% ($P{=}4$),
shown after-opt only in Fig.~\ref{fig:weak}.
Efficiency is below 100\% because at $P{=}1$ there is no allreduce, whereas
for $P{>}1$ every rank pays a fixed $\approx$15.6\,ms allreduce overhead that
does not shrink with local batch size, it is determined by the size of the gradient.
At $P{=}2$, weak efficiency (75\%) is lower than strong (80\%) because strong
scaling at $P{=}2$ gives each rank $B_\text{local}{=}8$, doubling the compute
per allreduce call. At $P{=}4$ both regimes coincide at $B_\text{local}{=}4$.

\section{Correctness and Verification}

\paragraph{Per-kernel.}
Every CUDA kernel is validated against a plain C++ CPU implementation
with tolerances $\text{rel}\leq 10^{-3}$ or $\text{abs}\leq 10^{-5}$.
The absolute guard prevents near-zero values from spuriously failing.
Forward kernels are additionally compared to PyTorch references.
All forward and backward tests pass. Table~\ref{tab:errors} reports the
achieved errors for each kernel. The PagedAttention decode kernel is
verified against the contiguous reference bit-identically
(abs $=$ 0, rel $=$ 0) across all seven test configurations, including
scrambled block-table orderings that would fail silently if the
indirection were ignored. Both also match a CPU oracle within FP32
\texttt{expf} tolerance.

\paragraph{Distributed training.}
We perform two multi-rank consistency checks. The step-1 loss is
bit-identical across $P\in\{1,2,4\}$ (6.5336), confirming that
\texttt{MPI\_Bcast} and the deterministic forward pass are correct. All
configurations drive loss toward the random-token entropy floor.
On Wikitext-2, the model trains correctly, falling from 9.27 to below
6.0 cross-entropy over 300 steps at $P{=}4$.

\section{Discussion}

\paragraph{Biggest payoffs.}
All four optimizations together cut 4-GPU step time by $\approx$15\,ms
(74.5$\to$59.8\,ms, $\approx$20\%). Register tiling ($4.3\times$ on
GEMM throughput) and \texttt{float4} (64--70\% of HBM peak) were
identified and quantified by Nsight Compute. The allreduce fusion and
scratch pre-allocation were only visible through Nsight Systems timeline
profiling. The former as PCIe staging between the GPU and host, the
latter as cudaMalloc barriers stalling the backward timeline.

\paragraph{Limitations and future work.}
The largest remaining lever is CUDA-aware MPI. The $\approx$8\,ms of
D$\to$H$\to$D PCIe staging per step would disappear over NVLink. Flash
Attention backward is $O(S^2)$ and FA2's recomputation
backward~\cite{dao2023} would be needed for longer sequences.
BF16~\cite{micikevicius2018} is not implemented because Turing has no
BF16 tensor cores, so BF16 arithmetic runs on regular CUDA cores at FP32
throughput. On Ampere/Hopper, native BF16 tensor cores would roughly
halve memory footprint and double matrix-multiply throughput relative to
FP32. Finally, our 5\,M-parameter model on Wikitext-2 reaches a
perplexity floor that is too high for coherent text generation. Scaling
to a full GPT-2 configuration (124\,M parameters) on a larger corpus
may give meaningful inference.

\begin{thebibliography}{9}

\bibitem{boehm2022}
S.\ Boehm.
How to Optimize a CUDA Matmul Kernel for cuBLAS-like Performance.
\textit{siboehm.com}, 2022.

\bibitem{cublas}
NVIDIA. cuBLAS Library User Guide. NVIDIA Developer Documentation, 2024.

\bibitem{dao2022}
T.\ Dao, D.\ Fu, S.\ Ermon, A.\ Rudra, and C.\ R\'{e}.
FlashAttention: Fast and Memory-Efficient Exact Attention with IO-Awareness.
In \textit{NeurIPS}, 2022.

\bibitem{dao2023}
T.\ Dao.
FlashAttention-2: Faster Attention with Better Parallelism.
\textit{arXiv:2307.08691}, 2023.

\bibitem{karpathy2024}
A.\ Karpathy. \texttt{llm.c}.
\url{https://github.com/karpathy/llm.c}, 2024.

\bibitem{kingma2015}
D.\ Kingma and J.\ Ba.
Adam: A Method for Stochastic Optimization.
In \textit{ICLR}, 2015.

\bibitem{kwon2023}
W.\ Kwon, Z.\ Li, S.\ Zhuang, Y.\ Sheng, L.\ Zheng, C.\ H.\ Yu,
J.\ E.\ Gonzalez, H.\ Zhang, and I.\ Stoica.
Efficient Memory Management for Large Language Model Serving with
PagedAttention. In \textit{SOSP}, 2023.

\bibitem{micikevicius2018}
P.\ Micikevicius et al.
Mixed Precision Training. In \textit{ICLR}, 2018.

\bibitem{radford2019}
A.\ Radford, J.\ Wu, R.\ Child, D.\ Luan, D.\ Amodei, and I.\ Sutskever.
Language Models are Unsupervised Multitask Learners.
OpenAI Technical Report, 2019.

\bibitem{sergeev2018}
A.\ Sergeev and M.\ Del Balso.
Horovod: Fast and Easy Distributed Deep Learning in TensorFlow.
\textit{arXiv:1802.05799}, 2018.

\bibitem{shoeybi2019}
M.\ Shoeybi, M.\ Patwary, R.\ Puri, P.\ LeGresley, J.\ Casper, and B.\ Catanzaro.
Megatron-LM: Training Multi-Billion Parameter Language Models Using
Model Parallelism. \textit{arXiv:1909.08053}, 2019.

\bibitem{williams2009}
S.\ Williams, A.\ Waterman, and D.\ Patterson.
Roofline: An Insightful Visual Performance Model.
\textit{CACM}, 52(4):65--76, 2009.

\end{thebibliography}

\appendix
\newpage
\section{All Implemented Kernels}
Table \ref{tab:all_kernels} shows an overview of every kernel we implemented.
\begin{table}[h]
\centering
\caption{Custom CUDA kernels implemented in this project.}
\label{tab:all_kernels}
\begin{tabular}{lll}
\toprule
\textbf{Kernel} & \textbf{Operation} & \textbf{Pass} \\
\midrule  
\multicolumn{3}{l}{\textit{Embedding \& loss}} \\
\texttt{embedding\_forward\_kernel}       & Token lookup              & Fwd \\
\texttt{cross\_entropy\_forward\_kernel}  & Softmax + CE loss         & Fwd \\
\texttt{cross\_entropy\_backward\_kernel} & CE gradient               & Bwd \\
\midrule
\multicolumn{3}{l}{\textit{Linear layers (GEMM)}} \\
\texttt{gemm\_nt\_reg\_kernel}            & $Y = X W^\top$            & Fwd \\
\texttt{gemm\_register\_tiled\_kernel}    & $dX = dY\, W$             & Bwd \\
\texttt{gemm\_tn\_reg\_kernel}            & $dW = X^\top dY$          & Bwd \\
\midrule
\multicolumn{3}{l}{\textit{Normalization}} \\
\texttt{layernorm\_forward\_kernel}       & LayerNorm                 & Fwd \\
\texttt{layernorm\_backward\_kernel}      & LayerNorm gradients       & Bwd \\
\midrule
\multicolumn{3}{l}{\textit{Activation}} \\
\texttt{gelu\_forward\_kernel}            & GELU activation           & Fwd \\
\texttt{gelu\_backward\_kernel}           & GELU gradient             & Bwd \\
\midrule
\multicolumn{3}{l}{\textit{Attention}} \\
\texttt{flash\_attention\_forward\_kernel}& Flash attention (tiled)   & Fwd \\
\texttt{softmax\_backward\_kernel}        & Attention softmax bwd     & Bwd \\
\texttt{attention\_weights\_kernel}       & $dQ, dK, dV$ gradients    & Bwd \\
\midrule
\multicolumn{3}{l}{\textit{Utility}} \\
\texttt{flat\_to\_bhsd\_kernel}           & Reshape to $[B,H,S,D]$    & Fwd/Bwd \\
\texttt{bhsd\_to\_flat\_kernel}           & Reshape to $[BS,H,D]$     & Fwd/Bwd \\
\texttt{add\_inplace\_kernel}             & Residual addition         & Fwd/Bwd \\
\midrule
\multicolumn{3}{l}{\textit{Optimizer \& inference}} \\
\texttt{adam\_update\_kernel}             & Adam optimizer step       & Opt. \\
\texttt{paged\_attention\_forward\_kernel}& Paged KV-cache attention  & Infer. \\
\bottomrule
\end{tabular}
\end{table}


\newpage
\section{Kernel Performance Details}\label{app:kernels}

\begin{table}[h]
  \centering\small\setlength{\tabcolsep}{5pt}
  \begin{tabular}{@{}llrrrr@{}}
    \toprule
    \textbf{Kernel} & \textbf{Bound} &
    \textbf{Baseline} & \textbf{\% peak} &
    \textbf{Optimized} & \textbf{\% peak} \\
    \midrule
    GEMM ($4096^3$)               & Compute & 2.20\,TFLOPS       & 13.5\% & 9.56\,TFLOPS & 58.6\% \\
    Flash Attn ($B{=}1$, $S{=}2048$) & Compute & ---             & ---    & 2.09\,TFLOPS & 12.8\% \\
    LayerNorm ($N{=}8192$)        & BW      & $\approx${}110\,GB/s & 16\%  & 432\,GB/s    & 64\%   \\
    GELU ($N{=}4$M)               & BW      & $\approx${}114\,GB/s & 17\%  & 471\,GB/s    & 70\%   \\
    Softmax ($V{=}30000$)         & BW      & ---                  & ---   & 431\,GB/s    & 64\%   \\
    Cross-Entropy ($V{=}30000$)   & BW      & ---                  & ---   & 472\,GB/s    & 70\%   \\
    Adam (5\,M params)            & BW      & ---                  & ---   & near-ceiling & ---    \\
    Paged Decode ($B{=}64$, $D{=}64$) & BW  & ---                  & ---   & 435\,GB/s    & 65\%   \\
    \bottomrule
  \end{tabular}
  \caption{Per-kernel performance: baseline vs.\ optimised on RTX\,6000
    (sm\_75). Peak: 16.31\,TFLOPS FP32, 672\,GB/s HBM. Baseline BW
    numbers for GELU/LayerNorm are from Nsight Compute before
    \texttt{float4} vectorisation; ``---'' means not in the before profile.
    Flash Attention at $B{=}1$ is low because 64 thread blocks
    underfill the 72 SMs; at training shape it saturates the device.
    Adam reads/writes $\approx$20 bytes/param and is bandwidth-dominated
    by construction.}
  \label{tab:kernels}
\end{table}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.92\linewidth]{roofline}
  \caption{Roofline for RTX\,6000 (sm\_75). FP32 ceiling 16.31\,TFLOPS;
    HBM ceiling 672\,GB/s; ridge $\approx$24\,FLOP/byte. Shown: both GEMM
    points (before/after register tiling), bandwidth-bound kernels after
    \texttt{float4}, and the decode regime ($\approx$0.5\,FLOP/byte).}
  \label{fig:roofline}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\linewidth]{plots/cudamalloc.png}
    \caption{Nsight Systems timeline \textbf{before} pre-allocation: repeated
      \texttt{cudaMalloc} calls stall the kernel queue, producing $\approx$30\,ms
      of idle GPU time per step.}
    \label{fig:cudamalloc}
\end{figure}

\begin{figure}[h]
    \centering
    \includegraphics[width=0.85\linewidth]{plots/cudamalloc_after.png}
    \caption{Nsight Systems timeline \textbf{after} pre-allocation: the
      \texttt{cudaMalloc} stalls are absent and the kernel queue runs
      without interruption.}
    \label{fig:cudamalloc_after}
\end{figure}

\newpage
\section{Communication Benchmark}\label{app:comm}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.88\linewidth]{comm_alpha_beta}
  \caption{\texttt{MPI\_Allreduce} latency vs.\ message size for $P{=}1$,
    $P{=}2$, and $P{=}4$, 100-trial averages. At small sizes latency is
    flat ($\alpha$-dominated, startup cost per call); at large sizes it
    grows with slope~1 ($\beta n$-dominated), implying an effective
    bandwidth of $\approx$3\,GB/s, constrained by the PCIe D$\to$H$\to$D
    round trip rather than the network. CUDA-aware MPI would bypass these
    copies.}
  \label{fig:comm}
\end{figure}

\newpage

\section{PagedAttention Benchmark}\label{app:paged}

\begin{table}[h]
  \centering\small\setlength{\tabcolsep}{6pt}
  \begin{tabular}{@{}rrrrrr@{}}
    \toprule
    $B$ & \textbf{naive util} & \textbf{paged util} &
    \textbf{naive step ($\mu$s)} & \textbf{paged step ($\mu$s)} &
    \textbf{overhead} \\
    \midrule
    1   & 100.0\% & 100.0\% & 15.0 &  15.6 & $+4.0\%$ \\
    4   &  43.6\% &  87.1\% & 15.1 &  15.8 & $+4.6\%$ \\
    16  &  51.9\% &  92.2\% & 17.9 &  18.7 & $+4.5\%$ \\
    64  &  39.4\% &  88.0\% & 38.0 &  38.5 & $+1.3\%$ \\
    256 &  44.4\% &  89.3\% & 129.2 & 130.7 & $+1.2\%$ \\
    \bottomrule
  \end{tabular}
  \caption{PagedAttention decode benchmark on a single RTX\,6000.
    Workload: $B$ concurrent requests, lengths $\sim$Exp(64) capped at
    128 tokens, $H{=}8$, $D{=}64$, block size 16, 200-iteration averages.
    \emph{Naive util}: useful KV / contiguous-padded allocation.
    \emph{Paged util}: useful KV / block-pool allocation. Indirection
    cost (last column) is $\leq$5\% across the sweep and converges to
    $\sim$1\% at large $B$ where the kernel saturates HBM bandwidth.}
  \label{tab:paged}
\end{table}

\newpage
\section{Weak Scaling}\label{app:weak}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.88\linewidth]{weak_scaling}
  \caption{Weak scaling efficiency (local $B{=}4$ per rank). Efficiency at
    $P{=}4$ is 73\%, driven by a nearly fixed comm cost ($\approx$15.6\,ms)
    regardless of batch size --- confirming that comm cost is dominated by
    gradient size, not compute volume.}
  \label{fig:weak}
\end{figure}



\end{document}
